# SoundCloud Desktop Proxy

Система проксирования для SoundCloud Desktop, использующая различные платформы (Cloudflare Workers / Deno Deploy / Node.js) для обхода блокировок.

## ⚠️ Важное обновление по блокировкам в России

**Deno Deploy заблокирован:** РКН перешел на новый этап блокировок - интернет по белым спискам. Для Deno Deploy пропускаются только первые **19 КБ** данных, после чего соединение удерживается без передачи данных. Это делает платформу непригодной для использования.

**Cloudflare Workers заблокирован:** Прямое подключение к Cloudflare Workers заблокировано на территории РФ.

### ✅ Рекомендуемое решение для России

**Двухуровневая схема проксирования:**

```
SoundCloud Desktop → Node.js (промежуточный прокси на RU-хосте) → Cloudflare Workers → SoundCloud
```

Эта схема использует:
1. **node-intermediate-proxy.js** - промежуточный прокси на российском хостинге/VPS
2. **cloudflare-proxy.js** - основной прокси на Cloudflare Workers

Промежуточный прокси на RU-хосте имеет доступ к Cloudflare Workers через белый список, решая проблему блокировки.

## Выбор платформы

### Cloudflare Workers

**Статус:** ⚠️ Заблокирован в России (требуется промежуточный прокси)

Cloudflare Workers заблокирован на территории РФ для прямых подключений, но доступен через промежуточный прокси на российском хостинге.

**Преимущества:**
- 🆓 Бесплатный тариф с щедрыми лимитами (100,000 запросов/день)
- ⚡ Быстрое развертывание
- 🌍 Глобальная сеть edge-серверов
- 🔄 Автоматическое масштабирование

**Использование в РФ:** Через двухуровневую схему с node-intermediate-proxy.js

### Deno Deploy

**Статус:** ❌ Заблокирован в России (РКН пропускает только 19 КБ)

[Deno Deploy](https://deno.com/deploy) больше **не рекомендуется** для использования в России. РКН блокирует соединения, пропуская только первые 19 КБ данных, что делает невозможным загрузку треков и другого контента.

**Проблема:** РКН использует блокировку по белым спискам - соединение устанавливается, но после передачи первых 19 КБ держится без передачи данных.

### Node.js Server (для самостоятельного хостинга)

**Статус:** ✅ Полный контроль

Собственный Node.js HTTP сервер для развертывания на своем VPS/сервере.

**Преимущества:**
- 🔒 Полный контроль над сервером
- 💰 Бесплатно (при наличии своего сервера)
- 🌐 Можно разместить в любой стране
- ⚙️ Настраиваемый порт и конфигурация
- 🚀 Поддержка стриминга и failover (node-intermediate-proxy.js)

> [!CAUTION]
> Deno workers кэшируют данные, поэтому в конфигурации прокси необходимо добавить в pathname {date} и {target}, чтобы не упереться в кэширование Deno.

## Принцип работы

### Базовое проксирование

1. **Клиент (SoundCloud Desktop)** отправляет запрос на прокси-сервер
2. **Целевой URL** кодируется в base64 и передается в заголовке `X-Target`
3. **Прокси** получает запрос, декодирует base64 и извлекает целевой URL
4. **Проксирование** - прокси делает запрос к целевому серверу и возвращает ответ
5. **Обработка редиректов** - прокси корректно обрабатывает редиректы, обновляя Location header

### Двухуровневая схема (для РФ)

```
┌─────────────────────┐
│ SoundCloud Desktop  │
│   (X-Target: URL)   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────────────┐
│  Node.js Intermediate Proxy │  ← На российском хостинге
│  (node-intermediate-proxy)  │     (доступен из РФ)
└──────────┬──────────────────┘
           │ X-Target: URL
           ▼
┌─────────────────────┐
│ Cloudflare Workers  │  ← В белом списке РКН
│  (cloudflare-proxy) │     для российских хостеров
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│     SoundCloud      │
└─────────────────────┘
```

**Преимущества:**
- ✅ Промежуточный прокси на RU-хосте имеет доступ к Cloudflare Workers
- ✅ Поддержка failover - можно использовать несколько Cloudflare Workers
- ✅ Стриминг данных без задержек
- ✅ Автоматическое переключение при rate limiting

### Зачем base64?

**Защита от DPI (Deep Packet Inspection)** - в России провайдеры анализируют содержимое HTTP-заголовков для блокировки запросов к определенным доменам. Base64-кодирование URL скрывает целевой домен от анализа трафика, делая блокировку более сложной.

## Структура файлов

- **cloudflare-proxy.js** - Cloudflare Worker для проксирования запросов
- **deno-proxy.js** - Deno Deploy worker (устарел, не работает в РФ)
- **node-proxy.js** - Node.js HTTP сервер для самостоятельного хостинга
- **node-intermediate-proxy.js** - Промежуточный Node.js прокси с поддержкой failover и стриминга
- **README.md** - Данная документация

## Настройка прокси

### URL Placeholders (опционально)

В URL прокси можно использовать специальные placeholder-ы для динамической генерации путей:

- **`{date}`** - заменяется на `Date.now()` (timestamp в миллисекундах)
- **`{target}`** - заменяется на base64-кодированный целевой URL

> **⚠️ ВАЖНО: Проблема автоматического кэширования Deno Deploy**
>
> Deno Deploy **автоматически кэширует ответы** на основе URL запроса. Без использования placeholders (например `{date}`) разные запросы к одному и тому же URL прокси будут возвращать **один и тот же закэшированный ответ**, независимо от того, какой целевой URL указан в заголовке `X-Target`.
>
> **Последствия:**
> - Запрос к `https://soundcloud.com/discover` (HTML) может вернуть содержимое JS-файла
> - Разные API endpoints возвращают одинаковые данные
> - Кэш может храниться **месяцами** (в логах видели `age: 9883765` = 114 дней!)
>
> **Решение:** Обязательно используйте placeholder `{date}` в URL для Deno Deploy, чтобы каждый запрос был уникальным и не использовал неправильный кэш.

**Пример использования:**
```json5
{
  "proxy": [
    // Базовый URL без placeholders
    "https://proxy.example.com",

    // URL с timestamp для обхода кэширования Deno Deploy
    "https://share-hawk-52.zxcnoname666.deno.net/{date}/{target}"
  ]
}
```

**Зачем нужны placeholders?**
- **`{date}`** - предотвращает кэширование responses на стороне Deno Deploy (каждый запрос уникален)
- **`{target}`** - добавляет уникальность URL для разных целевых адресов

**Результат:**
```
Запрос к: https://soundcloud.com/discover
Станет: https://share-hawk-52.loli669.deno.net/1732291234567/aHR0cHM6Ly9zb3VuZGNsb3VkLmNvbS9kaXNjb3Zlcg==
```

**Важно:** Прокси-сервер игнорирует path и продолжает работать по заголовку `X-Target` (основная логика без изменений).

### Вариант 1: Двухуровневая схема (✅ рекомендуется для РФ)

#### Шаг 1: Настройка Cloudflare Workers

1. Создайте новый Worker в Cloudflare Dashboard
2. Скопируйте содержимое `cloudflare-proxy.js` в редактор Worker
3. Опубликуйте Worker
4. Скопируйте URL вашего Worker (например: `https://soundcloud-proxy.your-subdomain.workers.dev`)

**Опционально:** Создайте несколько Workers для failover (разные аккаунты/subdomain'ы)

#### Шаг 2: Настройка промежуточного прокси на RU-хосте

1. Арендуйте VPS в России (любой дешевый хостинг с Node.js)
2. Установите Node.js 18+ на сервер:
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
   sudo apt-get install -y nodejs
   ```
3. Загрузите `node-intermediate-proxy.js` на сервер
4. Настройте переменные окружения:
   ```bash
   # Один прокси
   export PROXY_URL="https://soundcloud-proxy.your-subdomain.workers.dev"

   # Несколько прокси для failover (рекомендуется)
   export PROXY_URL="https://proxy1.workers.dev,https://proxy2.workers.dev,https://proxy3.workers.dev"

   # Порт (опционально, по умолчанию 3000)
   export PORT=3000
   ```
5. Запустите прокси:
   ```bash
   node node-intermediate-proxy.js
   ```
6. Настройте автозапуск через systemd или PM2:
   ```bash
   # Установка PM2
   npm install -g pm2

   # Запуск с автозапуском
   pm2 start node-intermediate-proxy.js --name soundcloud-proxy
   pm2 startup
   pm2 save
   ```

#### Шаг 3: Настройка SoundCloud Desktop

В конфигурационном файле приложения добавьте URL вашего промежуточного прокси:

```json5
{
  "proxy": [
    "http://your-ru-server.com:3000"
    // или с доменом
    // "https://proxy.your-domain.ru"
  ]
}
```

### Вариант 2: Node.js Server (самостоятельный хостинг)

1. Арендуйте VPS за рубежом (не в РФ)
2. Установите Node.js 18+
3. Загрузите `node-proxy.js` на сервер
4. Запустите:
   ```bash
   # По умолчанию порт 8080
   node node-proxy.js

   # Или с кастомным портом
   PORT=3000 node node-proxy.js
   ```
5. Настройте SoundCloud Desktop:
   ```json5
   {
     "proxy": [
       "http://your-server.com:8080"
     ]
   }
   ```

### Вариант 3: Deno Deploy (❌ не работает в РФ)

**Не рекомендуется:** РКН блокирует соединения после первых 19 КБ.

Если вы всё же хотите попробовать (для использования вне РФ):

1. Зарегистрируйтесь на [Deno Deploy](https://deno.com/deploy)
2. Создайте новый проект
3. В настройках проекта выберите "Deploy from file"
4. Загрузите файл `deno-proxy.js` или скопируйте его содержимое в онлайн-редактор
5. Опубликуйте проект
6. Скопируйте URL вашего Deno Deploy проекта (например: `https://your-project.deno.dev`)

## Технические детали

### Формат запросов

```
GET https://proxy.example.com/
Headers:
  X-Target: aHR0cHM6Ly9zb3VuZGNsb3VkLmNvbS9hcGkvdHJhY2tz
  (base64 кодированный URL: https://soundcloud.com/api/tracks)
  [остальные заголовки копируются как есть]
```

### Обработка редиректов

Прокси автоматически обрабатывает все типы редиректов:

- **Абсолютные URL** (`https://example.com/path`) - возвращаются как есть
- **Относительные пути** (`/path`) - преобразуются в абсолютные URL
- **Протокол-относительные** (`//example.com/path`) - дополняются протоколом

**Важно:** Location header в ответах редиректов теперь содержит прямую ссылку на целевой ресурс, а не обратно на прокси.

### Стриминг (node-intermediate-proxy.js)

Промежуточный прокси поддерживает стриминг данных:
- Данные передаются клиенту по мере получения от основного прокси
- Не требуется загрузка всего ответа в память
- Оптимально для больших файлов (аудио треки)
- Минимальная задержка перед началом воспроизведения

### Failover (node-intermediate-proxy.js)

Автоматическое переключение между прокси-серверами:
- При ошибке подключения переключается на следующий прокси
- При rate limiting (429, 503) пробует следующий прокси
- Перебирает все прокси из списка до успешного ответа
- Логирует используемый прокси для каждого запроса

### CORS поддержка

Прокси автоматически добавляют необходимые CORS заголовки:

```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS
Access-Control-Allow-Headers: *
```

### Очистка заголовков

Прокси автоматически удаляют потенциально проблемные заголовки:

- Платформо-специфичные заголовки (`cf-*` для Cloudflare, `x-deno-*` для Deno)
- Прокси заголовки (`x-forwarded-*`, `x-real-ip`)
- Заголовки безопасности (`content-security-policy`, `x-frame-options`)

## Примеры использования

### Базовый запрос

```javascript
const targetUrl = 'https://soundcloud.com/api/tracks';
const encodedUrl = btoa(targetUrl); // base64 encode

fetch('https://your-proxy.com/', {
    method: 'GET',
    headers: {
        'X-Target': encodedUrl
    }
})
```

### POST запрос с данными

```javascript
const targetUrl = 'https://soundcloud.com/api/upload';
const encodedUrl = btoa(targetUrl); // base64 encode

fetch('https://your-proxy.com/', {
    method: 'POST',
    headers: {
        'X-Target': encodedUrl,
        'Content-Type': 'application/json'
    },
    body: JSON.stringify({data: 'example'})
})
```

### Обработка ошибок

Прокси возвращает HTTP 400 если отсутствует заголовок `X-Target`:

```
HTTP/1.1 400 Bad Request
Missing X-Target header
```

Прокси возвращает HTTP 400 при невалидном base64:

```
HTTP/1.1 400 Bad Request
Invalid base64 encoded URL
```

Прокси возвращает HTTP 500/503 при ошибках проксирования:

```
HTTP/1.1 503 Service Unavailable
Proxy Error: [описание ошибки]
```

## Преимущества архитектуры

1. **Обход DPI** - base64 кодирование скрывает целевые URL от глубокой инспекции пакетов
2. **Обход блокировок РКН** - двухуровневая схема обходит блокировку Cloudflare Workers
3. **Отказоустойчивость** - failover между несколькими прокси-серверами
4. **Стриминг** - данные передаются без задержек и буферизации
5. **Простота конфигурации** - не нужно кодировать URL в пути
6. **Лучшая обработка редиректов** - корректные Location headers
7. **Совместимость** - работает с любыми HTTP методами и телами запросов
8. **Безопасность** - целевой URL скрыт от URL-строки и HTTP-логов

## Ограничения

### Cloudflare Workers

- Заблокирован для прямых подключений из России (требуется промежуточный прокси)
- Лимит на время выполнения (CPU time)
- Максимальный размер запроса/ответа ограничен платформой
- Некоторые заголовки могут быть изменены/удалены
- Бесплатный тариф: 100,000 запросов/день

### Deno Deploy

- ❌ **Заблокирован в России:** РКН пропускает только первые 19 КБ, затем держит соединение
- Лимит на время выполнения: 50ms CPU time на запрос
- Бесплатный тариф: 100,000 запросов/день, 100 ГБ исходящего трафика/месяц
- Может быть заблокирован в будущем в других странах

### Node.js Proxy

- Требуется собственный сервер/VPS
- Необходимость самостоятельной настройки и обслуживания
- Стоимость хостинга (обычно от $3-5/месяц)
- Нужно обеспечивать работоспособность и обновления

### Node.js Intermediate Proxy

- Требуется российский хостинг/VPS
- Дополнительная задержка (но минимальная благодаря стримингу)
- Зависимость от доступности основных прокси (Cloudflare Workers)
- Необходимость настройки нескольких прокси для надежности

## Рекомендации для пользователей из России

1. ✅ **Используйте двухуровневую схему:** node-intermediate-proxy.js на RU-хосте → Cloudflare Workers
2. ✅ **Настройте несколько Cloudflare Workers** для failover (2-3 штуки)
3. ✅ **Используйте дешевый российский VPS** для промежуточного прокси
4. ✅ **Настройте автозапуск** через PM2 или systemd
5. ⚠️ **Не используйте Deno Deploy** - заблокирован РКН (19 КБ лимит)

## Диагностика проблем

### Ошибка: "All proxy servers failed"

- Проверьте доступность Cloudflare Workers из вашего промежуточного прокси
- Убедитесь, что URL прокси указаны корректно в переменной PROXY_URL
- Проверьте логи промежуточного прокси для деталей ошибок

### Треки не загружаются / обрываются

- **Если используете Deno Deploy напрямую:** переключитесь на двухуровневую схему (Deno Deploy заблокирован)
- Проверьте, что промежуточный прокси работает и доступен
- Убедитесь, что используется стриминговый прокси (node-intermediate-proxy.js)

### Rate limiting (429 Too Many Requests)

- Настройте несколько Cloudflare Workers для failover
- Промежуточный прокси автоматически переключится на следующий при rate limiting
- Рассмотрите возможность использования платного тарифа Cloudflare Workers

## FAQ

**Q: Почему Deno Deploy больше не работает?**
A: РКН перешел на блокировку по белым спискам. Deno Deploy не входит в белый список, поэтому пропускаются только первые 19 КБ данных, после чего соединение держится без передачи данных.

**Q: Зачем нужен промежуточный прокси?**
A: Cloudflare Workers заблокирован для прямых подключений из России, но доступен для российских хостинг-провайдеров (они в белом списке РКН).

**Q: Сколько стоит российский VPS?**
A: Дешевые VPS с Node.js доступны от 150-300 руб/месяц. Этого достаточно для промежуточного прокси.

**Q: Можно ли использовать один Cloudflare Worker?**
A: Да, но рекомендуется настроить 2-3 для отказоустойчивости и обхода rate limiting.

**Q: Работает ли это с другими сервисами, кроме SoundCloud?**
A: Да, это универсальный HTTP прокси, который может проксировать любые HTTP/HTTPS запросы.
